{
  "name": "mac-spine-mock-redcap",
  "version": "1.0.0",
  "description": "Mock REDCap API — Cloudflare Worker that simulates REDCap for end-to-end testing",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.80.0"
  }
}
