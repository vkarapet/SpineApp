{
  "name": "mac-spine-proxy",
  "version": "1.0.0",
  "description": "MAC Spine API Proxy — Cloudflare Worker that relays assessment data to REDCap",
  "main": "dist/functions/*.js",
  "scripts": {
    "build": "tsc",
    "start": "func start",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
