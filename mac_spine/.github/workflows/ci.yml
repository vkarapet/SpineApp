name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mac_spine/package-lock.json
      - run: npm ci
        working-directory: mac_spine
      - run: npm run lint
        working-directory: mac_spine
      - run: npm run typecheck
        working-directory: mac_spine

  unit-tests:
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mac_spine/package-lock.json
      - run: npm ci
        working-directory: mac_spine
      - run: npm test
        working-directory: mac_spine

  build:
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mac_spine/package-lock.json
      - run: npm ci
        working-directory: mac_spine
      - run: npm run build
        working-directory: mac_spine
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: mac_spine/dist

  e2e-tests:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mac_spine/package-lock.json
      - run: npm ci
        working-directory: mac_spine
      - run: npx playwright install --with-deps
        working-directory: mac_spine
      - run: npm run test:e2e
        working-directory: mac_spine

  deploy-staging:
    runs-on: ubuntu-latest
    needs: [build, e2e-tests]
    if: github.ref == 'refs/heads/main'
    steps:
      - run: echo "Deploy to staging - configure with your hosting provider"

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    if: github.ref == 'refs/heads/main'
    environment: production
    steps:
      - run: echo "Deploy to production - requires manual approval"
